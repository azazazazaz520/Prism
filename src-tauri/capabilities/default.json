{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability",
  "windows": ["main", "floating", "import"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
